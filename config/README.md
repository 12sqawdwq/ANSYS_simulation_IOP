# 全局模型基线配置

`model_baseline.json` 是新实验眼睑厚度参考值的机器可读真源。当前统一基线为 **1.25 mm**。

运行器的普通压入、偏心和压力实验默认使用该值。厚度扫描、厚端网格无关性和厚度边界/敏感性实验可以显式使用其他厚度，但必须在配置和结果元数据中记录实际厚度及偏离基线的原因。

该配置只约束新运行的默认值，不重写历史输入、结果、图表或结论。完整解释见 [`../docs/GLOBAL_BASELINE.md`](../docs/GLOBAL_BASELINE.md)。
