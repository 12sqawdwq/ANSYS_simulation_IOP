# 模型与数据来源说明

当前报告包含两类证据。`thick/code/generate_placeholder_report.py` 生成的参数扫描用于比较眼睑厚度和角膜厚度趋势，压力分布不是从 ANSYS rst 直接提取；它只作为真实仿体实验开始前的占位资料。当前参数化三维有限元统一使用 `models/apdl/param_eye_sweep.mac`、`src/runners/run_indentation_sweep.py`、`models/apdl/post_sweep.mac` 和 `models/apdl/plot_sweep_views.mac`。模型先建立 IOP 预应力，再保持 IOP 施加探针压入；外侧接触面积来自闭合 CONTA174 单元面积求和，不再使用节点凸包代理。探头—眼睑最大数值穿透来自闭合接触单元的 `CONT:PENE` 平均值最大值。

| 项目 | 参数扫描 | 参数化 3D |
|---|---:|---:|
| IOP | 20 mmHg | 20 mmHg（2666.4 Pa） |
| 位移 | 0.29 mm | 0.00-0.80 mm，每 0.20 mm 采样 |
| 眼睑厚度 | 1.25 mm | 1.00 mm |
| 角膜厚度 | 0.55 mm | 0.60 mm |
| 眼睑-角膜 | 等效面积传递 | bonded |

两类结果只用于相互验证趋势，不直接比较绝对面积或压力。占位资料的 95% CI 来自参数扫描点，不是人群或重复实验统计区间。真实厚度实验的统计结果将以 `thick/data/processed/` 为唯一来源。

当前三维结果基于超弹性软组织、眼睑—角膜完全粘结、0.3 mm 主扫描网格和准静态加载。最大接触压力、角膜峰值应力和眼睑峰值应力只用于趋势比较。名义 0 mm 压入仍包含 0.05 mm 初始间隙闭合位移，不要求反力严格为零。

2026-07-20 边界检查中，`offset=2.0 mm, indent=1.0 mm` 在接近终点时出现 SOLID187 高畸变，2.0 mm 压入工况更早进入连续切步。因此当前统一流程将名义压入上限固定为 0.8 mm；1.0-2.0 mm 旧结果仅作为失稳边界证据，不进入正式趋势拟合。

提交 `649550f` 的四工况 smoke 全部一次完成，MAPDL 错误数为 0，批次 QC 无错误或警告。`offset=2.0 mm, indent=0.8 mm` 的探针反力为 `Fy=-0.55893 N`、`Fx=-0.05354 N`，闭合接触面积为 `7.5940 mm2`，面积加权接触中心为 `1.3365 mm`。四工况最大平均数值穿透为 `0.00629-0.01576 mm`，均低于 `0.03 mm` 警戒线；当前规范数据见 `results/summary/indentation_smoke.csv`。

完全粘结界面的完整面积和压力增量覆盖区不能直接解释为内部有效压平面积，因此不再输出旧的 `inner_area`、`Karea`、`DeltaParea` 或 `Parea`。`1°、2°、3°` 法向阈值和中心连续径向折点结果继续保留用于历史追溯及数值敏感性检查。

当前 GAT 主口径将外侧 `Ae` 定义为探头轴方向的平面几何足迹。模型角膜半径为 7.8 mm，眼睑外表面半径为 `R=7.8+t` mm，探头半径为 2.16 mm；名义推进 `δ` 下采用 `r=min(2.16,sqrt(2Rδ-δ²))` 和 `Ae=πr²`。该定义使推进达到球面弓高 `R-sqrt(R²-2.16²)` 时，`Ae` 自然达到探头面积。内侧 `Ac` 暂用中心连续径向折点内的轴向投影面积。

厚度结果同时保留 `gat_ae_area`、`contact_area`、`outer_surface_area` 和 `probe_area=14.6574 mm2`。它们分别回答“GAT 平面覆盖范围”“实际闭合承载范围”“外侧形变折点范围”和“探头上限”，不得互相替代。`0.35-0.80 mm` 接触扫描所见的 `0.70-0.75 mm` 平台仅表示当前材料和网格下的闭合承载面积饱和，不再称为 GAT 完全压平点。

提交 `bc861060` 的 12 工况 coarse 偏心扫描全部一次完成，批次 QC 为 0 error、0 warning。0.8 mm 推进时，偏心从 0 增加到 2.0 mm，基线扣除后的探头力从 0.7309 N 降至 0.5429 N，外侧闭合接触面积从 13.870 mm2 降至 7.594 mm2。完整评估见 `offset/docs/偏心压入粗扫描评估报告.md`，规范表见 `results/summary/indentation_coarse.csv`。

提交 `2ecc954` 的 7 工况眼睑厚度扫描全部一次完成，批次 QC 为 0 error、5 warning；warning 均来自内侧面积角度阈值敏感。厚度从 0.8 增加到 2.0 mm 时，探头反力从 0.6127 N 增至 1.4253 N，外侧闭合面积保持在 13.656-13.915 mm2，`Ae/Ac(2°)` 未复现旧占位资料的单调上升关系。完整评估见 `thick/docs/眼睑厚度有限元实验报告.md`。

专利汇报可围绕“外部探头响应—眼睑缓冲—角膜内侧有效压缩—眼压估计”的修正链条组织。眼睑厚度及 Ae/Ac 为主线，角膜厚度、探头偏心和接触质量为从属扩展。
