# 1.25 mm眼睑—0.60 mm角膜高眼压力学传递验证实验设计

## 1. 实验状态

- 状态：**仅完成预注册设计，尚未启动求解**。
- 实验目录：`high_iop_mechanical_transfer_t1p25_c0p60/`（与厚度研究目录 `thick/` 同级）
- 目标压力：`20、25、30、35、40 mmHg`，步长 `5 mmHg`。
- 高眼压实验点：5个。
- 零眼压结构参考：另设1个 `0 mmHg` 对照，不计入5个高眼压实验点。
- 当前阶段不根据新结果修改材料、5°面积阈值或预注册预测。

### 1.1 冻结的推进距离预设

本实验必须严格区分“求解终点”和“最终结论工作点”：

1. **ANSYS正式推进统一求解至 `0.28 mm`**，目的是使单个RST完整覆盖约0.26 mm主工作点及其后的推进路径；
2. **最终结论统一定位到最接近 `0.26 mm` 的实际RST结果集**，按当前时间步设置预计为 `0.259875 mm`；
3. `0.28 mm`只作为推进敏感性对照和结果追溯终点，不得替代约0.26 mm主结论；
4. 约0.26 mm状态必须直接读取RST结果集，不得由0.28 mm终点反力、面积或K插值得到；
5. 这里的推进量均指各自IOP预载后的几何初接触位置到探头目标位置的刚体平移，不是初始间隙加推进量、组织压缩量或完整接触距离。

冻结关系为：

$$
\boxed{
 d_{solve}=0.28\ \mathrm{mm},
 \qquad
 d_{primary}=0.259875\ \mathrm{mm}\approx0.26\ \mathrm{mm}
}
$$

## 2. 核心目的

本实验固定眼睑厚度和角膜厚度，验证当前几何初接触策略在 `20–40 mmHg` 高眼压区间内能否形成稳定、单调并可反演的力学传递关系。重点不是再次用实验读数反推系数，而是在已知输入IOP的FE环境中建立具有参考价值的仿真传感器系数。

本实验同时区分三类量：

1. `Kgeo,5° = Ae/Ac5°`：几何面积传播指标；
2. `Ksensor,Δ^FE`：相对零眼压结构参考的仿真虚拟传感器换算系数；
3. `Ksensor,total^FE`：直接使用总探头反力得到的诊断系数，只用于判断原始硬件读数和FE总反力是否为同一观测量。

主验证量定义为：

$$
\Delta F(t,d,p)=F(t,d,p)-F(t,d,0)
$$

这里的 $\Delta F$ 只解释为IOP改变造成的总耦合响应差，不解释为材料项被严格消除后的纯IOP反力。

完整探头面积口径的虚拟传感器压力为：

$$
\Delta P_{virtual}
=\frac{\Delta F}{A_{probe}}
$$

主仿真传感器系数为：

$$
K_{sensor,\Delta}^{FE}(t,d,p)
=\frac{p}{\Delta P_{virtual}(t,d,p)}
$$

完整探头面积固定为：

$$
A_{probe}=14.6574147\ \mathrm{mm^2}
$$

## 3. 预注册假设

### H1：反力和虚拟传感器压力保持单调

在20–40 mmHg内，固定厚度和推进量后，预计：

- 总轴向反力 $F$ 随IOP增加；
- $\Delta P_{virtual}$ 随IOP增加；
- 相邻5 mmHg压力区间的增益保持为正。

### H2：高压区响应继续饱和

已有1.25 mm结果显示，压力升高后相邻区间力学增益下降。因此预计：

$$
\frac{\Delta P(25)-\Delta P(20)}{5}
>
\frac{\Delta P(40)-\Delta P(35)}{5}
$$

若局部增益降至接近零或出现负值，应判定当前单值反演策略在该区间失效。

### H3：Ksensor随压力升高

已有10/20/30 mmHg结果支持低压下K较低、高压下K增大。当前优先检验的探索性形式为：

$$
K_{sensor,\Delta}^{FE}(p)=\alpha+\beta p,
\qquad \beta>0
$$

### H4：0.26 mm工作点的K高于0.28 mm

九厚度结果中，实际 `0.259875 mm` 相比 `0.28 mm` 的Ksensor平均提高约 `7.5%`。预计本组高压结果仍满足：

$$
1.04
\le
\frac{K_{0.259875}}{K_{0.28}}
\le
1.12
$$

### H5：Kgeo不能直接代替Ksensor

本实验会同步提取Ae、Ac5°和Kgeo，但不预设Kgeo与Ksensor具有固定比例。只计算二者的Pearson和Spearman相关性，不用相关结果反调5°阈值。

## 4. 实验矩阵

| 类型 | case_id | IOP/mmHg | IOP/Pa（近似） | 用途 |
|---|---|---:|---:|---|
| 结构参考 | `high_iop_t1p25_c0p60_iop000_d0p28` | 0 | 0.0 | 计算同批次 $F_0$；不计入高眼压5点 |
| 高压点 | `high_iop_t1p25_c0p60_iop020_d0p28` | 20 | 2666.45 | 历史锚点复现、区间起点 |
| 高压点 | `high_iop_t1p25_c0p60_iop025_d0p28` | 25 | 3333.06 | 新增插值验证点 |
| 高压点 | `high_iop_t1p25_c0p60_iop030_d0p28` | 30 | 3999.67 | 历史锚点复现 |
| 高压点 | `high_iop_t1p25_c0p60_iop035_d0p28` | 35 | 4666.28 | 新增外推验证点 |
| 高压点 | `high_iop_t1p25_c0p60_iop040_d0p28` | 40 | 5332.89 | 新增高压端验证点 |

所有工况统一求解至几何初接触后的 `0.28 mm`，保留完整RST。后处理从同一个RST中读取：

- 实际 `0.259875 mm`：当前主几何工作点；
- `0.28 mm`：固定推进敏感性对照。

不得用0.28 mm终点插值得到0.259875 mm反力或面积。

## 5. 几何建模参数

| 参数 | 固定值 |
|---|---:|
| 角膜外表面曲率半径 $R_c$ | `7.80 mm` |
| 角膜厚度 | `0.60 mm` |
| 角膜内表面半径 | `7.20 mm` |
| 眼睑厚度 | `1.25 mm` |
| 眼睑外表面半径 | `9.05 mm` |
| 模型径向范围 | `6.00 mm` |
| 探头半径 | `2.16 mm` |
| 探头直径 | `4.32 mm` |
| 探头完整面积 | `14.6574147 mm²` |
| 探头高度 | `4.00 mm` |
| 探头横向偏置 | `0 mm`，同轴加载 |
| 未加载初始间隙 | `0.30 mm` |
| 正式终点推进量 | `0.28 mm`，从各自IOP下的几何初接触位置起算 |
| 主后处理推进状态 | `0.259875 mm`，取RST实际结果集 |

几何由角膜球壳、等厚眼睑层和刚性近似平底圆柱探头构成。眼睑与角膜完全粘结；探头—眼睑界面采用标准法向接触，不引入额外摩擦参数。

## 6. 材料、网格和边界参数

### 6.1 单元与网格

| 项目 | 设置 |
|---|---|
| 实体单元 | `SOLID187`四面体 |
| 接触单元 | `TARGE170 / CONTA174` |
| 标准网格尺寸 | `0.30 mm` |
| 网格策略 | 与当前九厚度几何零点基线一致 |
| 细网格 | 本组主实验暂不并行增加；通过后另做代表点验证 |

### 6.2 超弹性材料

采用当前Mooney–Rivlin参数，不根据高压结果反调材料：

| 组织 | 基础C10 | 基础C01 | 材料倍率 | 本实验有效C10 | 本实验有效C01 | D1 |
|---|---:|---:|---:|---:|---:|---:|
| 眼睑 | `0.076 MPa` | `0.010 MPa` | `1.00` | `0.076 MPa` | `0.010 MPa` | `0.1×10⁻⁶ Pa⁻¹` |
| 角膜 | `0.110 MPa` | `0.025 MPa` | `0.75` | `0.0825 MPa` | `0.01875 MPa` | `0.1×10⁻⁶ Pa⁻¹` |

探头采用线弹性材料：

- 弹性模量：`3500 MPa`；
- 泊松比：`0.33`。

### 6.3 约束和压力

- 角膜外围约6 mm半径区域固定；
- IOP施加于角膜内表面；
- 眼睑—角膜界面为完全粘结；
- 压力预载阶段探头必须保持无接触；
- 每一压力都独立执行几何初接触，不共用20 mmHg的接触零点。

## 7. 加载与求解流程

每个压力工况执行相同的三个载荷步：

1. **IOP预载**：探头保持初始分离，建立对应压力的预应力和顶点抬升；
2. **几何接近**：根据该压力下顶点抬升量移动探头，到达几何初接触；
3. **正式推进**：从几何初接触位置继续刚体平移 `0.28 mm`。

求解设置：

| 项目 | 设置 |
|---|---|
| 分析类型 | 三维静力、大变形 `NLGEOM,ON` |
| 非线性算法 | Full Newton–Raphson |
| 线搜索 | 开启 |
| 自动时间步 | 开启 |
| 稳定化 | 常能量比 `1.0×10⁻⁴` |
| 力收敛容差 | 当前模型 `CNVTOL,F,0.01` |
| 标准核数 | 每工况8核 |
| 单工况超时 | `3600 s` |
| 并发上限 | 最多2个工况，每个8核 |
| 重试 | 最多2次；重试只缩小初始子步，不修改物理参数 |
| 大文件保留 | DB、RST和最终日志保存在仓库外 `blueknow-data` |

## 8. 必须提取的观测量

### 8.1 力学与虚拟传感器

- 探头总轴向反力 $F(p,d)$，单位N；
- 相对同批次0 mmHg参考的 $\Delta F$；
- 完整探头等效总压力；
- $\Delta P_{virtual}$；
- `Ksensor,Δ^FE`；
- `Ksensor,total^FE`，仅作诊断；
- 相邻5 mmHg区间的压力增益和局部逆增益；
- 探头反力与接触压力积分的一致性。

### 8.2 几何与接触

- IOP预载顶点抬升量；
- 几何接近距离；
- 预载和几何接近阶段的接触节点数、接触面积和探头反力；
- 外侧 $A_e$；
- 内侧 $A_{c,5^\circ}$；
- $K_{geo,5^\circ}=A_e/A_c$；
- 闭合接触承载比例；
- 最大接触压力和最大穿透量。

## 9. 当前预注册预测

现有1.25 mm厚度的10和30 mmHg结果给出探索性线性K模型。

### 9.1 实际0.259875 mm工作点

$$
K_{sensor,\Delta}^{FE}(p)
=1.795854+0.0670603p
$$

### 9.2 0.28 mm对照工作点

$$
K_{sensor,\Delta}^{FE}(p)
=1.658717+0.0625427p
$$

这些关系只作为本实验开始前冻结的预测，不代表已验证的40 mmHg外推模型。

| IOP/mmHg | Ksensor预计值，0.259875 mm | $\Delta P_{virtual}$预计/mmHg | Ksensor预计值，0.28 mm | $\Delta P_{virtual}$预计/mmHg |
|---:|---:|---:|---:|---:|
| 20 | `3.137`；历史实际`3.057` | 模型`6.375`；历史实际`6.542` | `2.910`；历史实际`2.828` | 模型`6.874`；历史实际`7.072` |
| 25 | `3.472` | `7.200` | `3.222` | `7.758` |
| 30 | `3.808`；历史实际`3.808` | `7.879` | `3.535`；历史实际`3.535` | `8.487` |
| 35 | `4.143` | `8.448` | `3.848` | `9.096` |
| 40 | `4.478` | `8.932` | `4.160` | `9.614` |

考虑模型、网格和高压外推不确定性，预期合理区间暂定为：

| IOP/mmHg | Ksensor合理范围，0.259875 mm | Ksensor合理范围，0.28 mm |
|---:|---:|---:|
| 20 | `2.95–3.25` | `2.70–3.00` |
| 25 | `3.25–3.75` | `3.00–3.45` |
| 30 | `3.60–4.05` | `3.35–3.75` |
| 35 | `3.85–4.55` | `3.60–4.25` |
| 40 | `4.15–5.10` | `3.85–4.75` |

几何K只作次级预测：

- 0.259875 mm下，预计Kgeo由20 mmHg的约`2.63`增加到40 mmHg的约`2.9–3.1`；
- 0.28 mm下，预计Kgeo由20 mmHg的约`2.42`增加到40 mmHg的约`2.5–2.7`；
- 不要求Kgeo与Ksensor数值相等。

## 10. 相关性分析计划

对0.259875和0.28 mm分别计算：

1. IOP与总反力的Pearson、Spearman相关系数；
2. IOP与 $\Delta P_{virtual}$ 的相关系数；
3. IOP与Ksensor的相关系数；
4. IOP与预载顶点抬升量的相关系数；
5. IOP与Ac5°、Kgeo的相关系数；
6. Kgeo与Ksensor的相关系数；
7. 相邻区间力学增益与压力的相关性；
8. 两推进工作点之间Ksensor的配对差异。

预期：

- IOP与总反力、$\Delta P_{virtual}$、顶点抬升量均强正相关；
- Ksensor与IOP强正相关；
- 相邻区间增益与IOP负相关；
- Ac5°与IOP负相关；
- Kgeo与IOP正相关，但Kgeo和Ksensor不保证固定比例。

## 11. 通过、部分通过与失败标准

### 11.1 数值质量前置条件

每个工况必须同时满足：

- `status=complete`；
- 返回码为0；
- ANSYS错误数为0；
- 三个载荷步全部收敛；
- IOP预载和几何接近阶段无提前接触；
- 几何初接触反力接近0；
- 0.259875和0.28 mm结果集均可直接读取；
- 反力与接触压力积分差异原则上不超过1%。

### 11.2 当前策略通过

同时满足：

- 20–40 mmHg范围内 $\Delta F$ 严格单调增加；
- 所有相邻5 mmHg区间增益为正；
- 25、35、40 mmHg相对预注册K预测的偏差原则上不超过10%；
- 使用预注册模型反演新压力点时，MAE不超过`2 mmHg`，且单点绝对误差不超过`3 mmHg`；
- 0.259875/0.28 mm的K比值保持在`1.04–1.12`附近；
- 不出现接触拓扑突变或数值穿透异常。

### 11.3 部分通过

- 反力和压力响应保持单调，可唯一反演；
- 但K随压力呈明显曲率，线性K模型误差超过10%；
- 此时应升级为形状保持分段插值或饱和响应模型，不得强行保留固定K或线性K。

### 11.4 失败

出现任一情况：

- 高压区 $\Delta F$ 不再单调；
- 相邻压力点产生相同或反向虚拟传感器输出；
- 几何初接触在高压下失效；
- 接触状态突变导致单值K无法定义；
- 反演误差超过预设范围且不能由数值质量解释。

## 12. 预计结论

在当前模型下，预计20–40 mmHg中总反力仍增加，但增量逐渐饱和；因此固定单一K不能覆盖整个高眼压区间。最可能的结果是：

1. `Ksensor,Δ^FE` 随IOP升高，从约`3.0`上升到约`4.2–4.8`；
2. 0.259875 mm工作点的K比0.28 mm高约`5%–10%`；
3. 25 mmHg仍接近插值区，误差较小；
4. 35和40 mmHg将决定线性K策略能否外推；
5. 若40 mmHg响应比预测更早饱和，K会高于当前上限；
6. Kgeo预计缓慢增加，但不能替代力学定义的Ksensor；
7. 若采用未经结构参考扣除的总探头压力，则所得 `Ksensor,total^FE` 将远小于1，不能与预期直接乘硬件读数的K混用。

## 13. 误差分析与来源

### 13.1 数值误差

- `0.30 mm`标准网格造成的反力、接触面积和5°边界离散误差；
- 0.259875 mm与理想0.260000 mm之间的`0.125 μm`状态误差；
- 自动时间步在不同IOP下产生的结果集位置差异；
- 接触穿透、接触刚度和稳定化能量对高压状态的影响；
- 高压下局部接触压力梯度增大，使面积和压力积分更敏感；
- 并行求解和结果读取的微小浮点差异。

### 13.2 模型形式误差

- K随压力未必严格线性；
- 当前线性K形式是在既有20 mmHg结果被观察后选出的探索模型，20 mmHg不能继续充当完全独立的模型选择盲点；
- 由30 mmHg外推至35和40 mmHg可能低估响应饱和，从而低估K；
- 单一厚度结果不能直接推广到0.80–2.00 mm全厚度区间。

### 13.3 物理模型误差

- 眼睑和角膜Mooney–Rivlin参数的不确定性；
- 眼睑—角膜完全粘结可能高估实际传力；
- 约6 mm固定边界可能放大局部结构刚度；
- 几何采用同心球壳，未包含真实个体非球面、眼睑纤维方向和组织分层；
- 未建模泪膜、眼睑滑移、摩擦和组织黏弹性；
- 高IOP下材料参数可能具有应变率或压力依赖性。

### 13.4 虚拟传感器与硬件映射误差

- 暂以完整探头面积作为固件换算面积，真实有效面积尚未冻结；
- ZNLBS传感器测量轴向力，但硬件显示mmHg时的固件比例和零点策略尚未完整编码；
- 探头到力传感器之间可能存在结构传递损失；
- 实验7.5–8.5 mmHg读数可能是总读数，而本实验主K使用零眼压结构参考扣除后的增量读数；
- 若两种零点不同，仿真Ksensor不能直接乘实验原始读数。

### 13.5 统计误差

- 每个IOP目前只有一个确定性FE工况，相关系数主要描述模型趋势，不代表人群统计置信度；
- 五个高压点样本量有限，Pearson相关可能掩盖曲率；
- 应同时报告Spearman相关、区间增益和残差，不以单一相关系数判定模型通过。

## 14. 执行约束

- 本设计批准前不启动求解；
- 正式运行必须写入独立的仓库外数据目录；
- 20和30 mmHg历史结果只用于预注册预测和复现QC，正式评分优先使用同批次新结果；
- 25、35、40 mmHg在结果揭示前不得修改预注册预测；
- 不根据本实验反调5°角度；
- 不在0.80–2.00 mm厚度范围外外推；
- 不将 `Kgeo`、`Ksensor,Δ` 和 `Ksensor,total` 混写为同一个K；
- 实验完成后先进行终端审查，得到批准后再更新正式报告。
