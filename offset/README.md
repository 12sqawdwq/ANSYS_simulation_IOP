# Offset Study

本目录是偏心测量研究的唯一归档位置。

- `docs/`：偏心参数扫描和参数化 3D 结果说明。
- `data/`：轻量汇总 CSV。
- `figures/`：偏心与零偏心对比视图。
- `code/`：偏心后处理宏及历史 Workbench 脚本。
- `workbench/`：本地 1 mm 偏心工程，Git 忽略。

其中的半经验面积和 3D 接触单元代理属于不同证据层级，不能直接互换为绝对面积。
